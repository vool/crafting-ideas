import React from 'react';

const navBarStyle = {
  top: '0',
  width: '100%',
  height: '65px',
  background: '#4A708B'
};

const NavBar = () => {
  return <div style={navBarStyle} />;
};

export default NavBar;
